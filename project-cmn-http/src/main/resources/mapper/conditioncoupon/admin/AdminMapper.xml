<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.cmn.http.persistence.mapper.conditioncoupon.AdminMapper">
    <select id="selectAdmin" resultType="com.project.cmn.http.persistence.model.conditioncoupoon.AdminModel">
        SELECT ADMIN_NUM
             , ADMIN_ID
             , PASSWD
             , ADMIN_NM
             , ROLE_ID
             , EMP_NO
             , EMAIL
             , TEL
             , HP_NUM
             , LAST_LOGIN_DT
             , PWD_INIT_YN
             , PWD_UPD_DT
             , LOGIN_FAIL_CNT
             , LOGIN_FAIL_DT
             , VALID_YN
             , REG_DT
             , REG_ID
             , UPD_DT
             , UPD_ID
        FROM TB_ADMIN
        WHERE ADMIN_ID = #{adminId}
    </select>
</mapper>
